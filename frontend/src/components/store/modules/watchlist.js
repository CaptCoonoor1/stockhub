const state = {
	// watchlist: [],
};

const mutations = {
	// SET_STOCKS(state, watchlist) {
	// 	state.watchlist = watchlist;
	// },
};

const actions = {
	// remove({ commit }, stock) {
	// 	commit('REMOVE_FROM_WATCHLIST', stock);
	// },
};

export default {
	// state,
	// mutations,
	// actions,
	// getters,
};
